# Changelog

All notable changes to `laravel-ts-annotations` will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## Unreleased

- Initial release: `#[TS]`, `#[TSType]` and `#[TSEnum]` attributes, the
  `ts:generate` command (`--output`, `--dry-run`), configurable scan paths,
  multiple output files, and marker-based file preservation.
